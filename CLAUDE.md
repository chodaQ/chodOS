# MuKernel 개발 규칙 (Claude Code 자동 적용)

이 프로젝트는 단순 학습용이 아니라, 나중에 논문/기술 문서로 정리할 수도
있는 연구 기록을 남기는 중이다. 아래 규칙을 항상 따를 것.

---

## 1. 실험/측정 기록 규칙 (가장 중요)

성능 측정, A/B 비교, 벤치마크를 할 때마다 **반드시 항상. 또한 수정된 사항이나 그런것들 모두다** 프로젝트 루트의
`EXPERIMENTS.md`에 다음 형식으로 기록한다. 파일이 없으면 새로 만든다.

```markdown
## 실험 N: (제목)

**날짜:** YYYY-MM-DD
**가설:** 무엇을 확인하려고 했는가

**측정 환경:**
- 호스트: Apple M4 Pro (또는 실제 호스트)
- QEMU 버전, `-smp` 옵션 (코어 수)
- cargo build 모드 (debug/release, 빌드 플래그)
- 측정 도구 (예: rdtsc), 샘플 수(n=)

**방법:** 정확히 무엇을 어떻게 측정했는지 (코드 스니펫 포함 권장)

**Raw 데이터:**
평균/min/max뿐 아니라 가능하면 전체 샘플 데이터를 표나 리스트로 남긴다.
(평균만 남기면 나중에 분산/신뢰구간 분석이 불가능해짐)

**결과 해석:** 숫자가 무엇을 의미하는지, 왜 이렇게 나왔는지

**다음에 미친 영향:** 이 결과가 다음 설계/실험 결정에 어떻게 반영됐는지
```

**예상과 다르거나 실패한 결과도 절대 누락하지 말 것.** (예: BETA-X 7에서
fast channel이 오히려 0.9×로 느렸던 것, A-1에서 crossover가 예상과
반대로 64B에서만 유리했던 것 — 이런 "반전"이 가장 가치 있는 기록이다.)

---

## 2. 작업 전후 체크리스트

- 큰 변경을 시작하기 전에는 `ARCHITECTURE.md`에서 관련 섹션을 먼저 읽고
  지금 단계(BETA-X-2 등)와 설계 원칙에 맞는지 확인한다.
- 작업이 끝나면 `ARCHITECTURE.md`/`README.md`에 상태 갱신이 필요한지
  확인하고, 필요하면 먼저 사용자에게 요약해서 알린다 (직접 덮어쓰지 않음 —
  이 두 파일은 사용자가 Claude(claude.ai)와 같이 다듬는 문서이므로, Claude
  Code는 진행 상황을 보고하는 역할까지만 하고 실제 문서 수정은 사용자가
  Claude와 함께 한다). 
- `EXPERIMENTS.md`는 Claude Code가 직접 갱신해도 된다 (실험 원본 기록이므로).

---

## 3. 코드/주석 규칙

- 디버그용 임시 마커(예: 특정 레지스터에 매직 바이트 쓰기)는 작업이 끝나면
  반드시 제거한다. (과거 `rax` 오염 버그가 이런 디버그 마커 때문에 발생한 적
  있음 — `ARCHITECTURE.md` 참고)
- 주석은 "왜 이렇게 했는지"를 한국어로 충분히 남긴다. 이 프로젝트는 학습/연구
  목적이 크므로 코드를 나중에 다시 읽었을 때 이해 가능해야 한다.
- 빌드 경고(warning)는 방치하지 않는다. 발생하면 즉시 정리하거나, 의도적으로
  남기는 경우 그 이유를 주석으로 남긴다.

---

## 4. 보안/안전 설계 원칙 (BETA-X-2 이후 전체에 적용)

"생성은 엄격하게, 사용은 가볍게" 원칙을 유지한다:
- 채널/리소스 생성 시점에는 Capability 검증, 권한 매핑을 엄격히 한다.
- 생성된 이후의 반복적인 사용(매 프레임, 매 클릭 등)은 가볍게 만들어도
  되지만, 그로 인해 생기는 검증 공백은 별도의 감시(이상 탐지, decay,
  Event Tracer 기록)로 사후에 메운다.
- 속도를 위해 안전장치를 생략하는 결정을 내릴 때는, 그 트레이드오프를
  `EXPERIMENTS.md`에 명시적으로 기록한다.

---

## 5. 현재 진행 상태 참고

최신 상태는 항상 `ARCHITECTURE.md` 최상단 요약을 참고할 것. 이 파일
(`CLAUDE.md`)은 "어떻게 작업할지"에 대한 규칙이고, "지금 무엇을 하고
있는지"는 `ARCHITECTURE.md`가 기준이다.

## 6. 항상 진행후 progress.json에 반영

현재 완료 상대 알아보기 위함



**중요**
❯ 너가 아키.md 업데이트하고 이후 로드맵 생각해서 작성** 위에 무시