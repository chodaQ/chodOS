# 기여 가이드

ChodOS에 관심 가져주셔서 감사합니다. 이 문서는 이 저장소가 어떤 방식으로
운영되는지, 기여하려면 무엇을 알아야 하는지를 정리한 것입니다.

## 이 프로젝트의 성격

ChodOS는 Rust로 밑바닥부터 만드는 x86_64 마이크로커널입니다. 단순한 학습용
토이 프로젝트가 아니라 **모든 성능 주장을 직접 측정해 기록으로 남기는 것**을
원칙으로 합니다. 그래서 코드만큼이나 `EXPERIMENTS.md`가 중요합니다.

- `README.md` — 프로젝트 개요와 현재 단계
- `ARCHITECTURE.md` — 설계 원칙, 마일스톤, 로드맵
- `EXPERIMENTS.md` — 40여 회의 실험 원본 기록 (실패·반전 결과 포함)
- `roadmap.json` — 진행 상황 추적

## 개발 환경

```bash
rustup toolchain install nightly     # rust-toolchain.toml이 자동 적용됨
brew install qemu xorriso            # macOS 기준
```

**QEMU는 11.0.3 이상을 권장합니다.** 11.0.0에서는 타이머/yield ISR의
`iretq`에서 `#GP` fault가 재현되는 문제가 있었습니다(자세한 내용은
`EXPERIMENTS.md` 실험 40~42 참고).

```bash
make kernel     # 커널만 빌드
make all        # 부팅 가능한 ISO 생성
make run        # QEMU 실행 (시리얼 콘솔)
make run-gui    # QEMU 실행 (GUI 포함)
```

## 기여 절차

1. 이슈를 먼저 열어주세요. 무엇을 왜 바꾸려는지 짧게 적으면 됩니다.
2. `main`에서 브랜치를 따서 작업합니다. (`fix/...`, `feat/...`, `exp/...`)
3. PR을 열어주세요. CI(커널 빌드)가 통과해야 병합됩니다.

## 코드 규칙

- **주석은 "왜 이렇게 했는지"를 한국어로 충분히 남깁니다.** 나중에 다시
  읽었을 때 이해할 수 있어야 합니다. "무엇을 하는지"는 코드가 이미 말해줍니다.
- **컴파일 경고를 방치하지 않습니다.** 의도적으로 남기는 경우 그 이유를
  주석으로 적습니다.
- 디버그용 임시 마커(특정 레지스터에 매직 값 쓰기 등)는 작업이 끝나면
  반드시 제거합니다. 과거 이런 마커 때문에 레지스터 오염 버그를 겪은 적이
  있습니다.

## 성능을 주장하려면 측정이 필요합니다

"빨라진 것 같다"는 이 저장소에서 받아들여지지 않습니다. 성능 변화를
주장하는 PR은 `EXPERIMENTS.md`에 다음 형식으로 기록을 남겨주세요.

```markdown
## 실험 N: (제목)

**날짜:** YYYY-MM-DD
**가설:** 무엇을 확인하려고 했는가

**측정 환경:** 호스트, QEMU 버전, -smp 옵션, 빌드 모드, 측정 도구, 샘플 수(n=)
**방법:** 정확히 무엇을 어떻게 측정했는지
**Raw 데이터:** 평균만이 아니라 가능하면 전체 샘플
**결과 해석:** 숫자가 무엇을 의미하는지
**다음에 미친 영향:** 이 결과가 다음 결정에 어떻게 반영됐는지
```

**예상과 다르거나 실패한 결과일수록 더 가치가 있습니다.** 실제로 이
프로젝트에서 가장 중요한 발견들은 대부분 "예상과 반대로 나온 결과"에서
나왔습니다.

- IPC fast channel이 오히려 느렸던 것 (실험 A-1/BETA-X 7)
- CFS를 기본 스케줄러로 올렸다가 소비자 기아로 OOM이 나서 되돌린 것 (실험 45)
- `#GP` 회귀의 원인 분석이 두 차례 틀렸다가 로그 대조로 바로잡힌 것 (실험 45)

이런 기록을 지우지 않는 것이 이 프로젝트의 핵심 원칙입니다.

## 라이선스

기여하신 코드는 [MIT License](LICENSE)로 배포됩니다.
